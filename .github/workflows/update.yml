name: Auto-update cliamp

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check for new release
        id: version
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LATEST=$(gh api repos/bjarneo/cliamp/releases/latest --jq '.tag_name')
          LATEST_SEMVER="${LATEST#v}"
          CURRENT=$(jq -r 'max_by(.version | split(".") | map(tonumber)) | .version' data/cliamp.json)
          echo "latest=${LATEST}" >> "$GITHUB_OUTPUT"
          echo "latest_semver=${LATEST_SEMVER}" >> "$GITHUB_OUTPUT"
          echo "current=${CURRENT}" >> "$GITHUB_OUTPUT"
          if jq -e --arg v "$LATEST_SEMVER" 'any(.[]; .version == $v)' data/cliamp.json >/dev/null; then
            echo "Version ${LATEST_SEMVER} already in data/cliamp.json, nothing to do."
            echo "up_to_date=true" >> "$GITHUB_OUTPUT"
          elif gh pr list --head "auto-update/cliamp-${LATEST_SEMVER}" --json number --jq 'length' | grep -q '^[1-9]'; then
            echo "Open PR already exists for ${LATEST}, nothing to do."
            echo "up_to_date=true" >> "$GITHUB_OUTPUT"
          else
            echo "New version available: ${CURRENT} -> ${LATEST_SEMVER}"
            echo "up_to_date=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Nix
        if: steps.version.outputs.up_to_date == 'false'
        uses: cachix/install-nix-action@v27
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Append new release to data/cliamp.json
        if: steps.version.outputs.up_to_date == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: nix run path:.#update

      - name: Refresh flake.lock
        if: steps.version.outputs.up_to_date == 'false'
        run: nix flake update

      - name: Update README current version
        if: steps.version.outputs.up_to_date == 'false'
        run: |
          LATEST="${{ steps.version.outputs.latest }}"
          sed -i \
            -e "s|cliamp \[v[^]]*\](https://github.com/bjarneo/cliamp/releases/tag/v[^)]*)|cliamp [${LATEST}](https://github.com/bjarneo/cliamp/releases/tag/${LATEST})|" \
            README.md

      - name: Build and smoke-test
        if: steps.version.outputs.up_to_date == 'false'
        run: |
          nix build path:.#cliamp
          EXPECTED="cliamp version ${{ steps.version.outputs.latest }}"
          ACTUAL=$(./result/bin/cliamp -v)
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "ERROR: expected '${EXPECTED}', got '${ACTUAL}'"
            exit 1
          fi
          echo "OK: ${ACTUAL}"

      - name: Open PR
        if: steps.version.outputs.up_to_date == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LATEST="${{ steps.version.outputs.latest }}"
          LATEST_SEMVER="${{ steps.version.outputs.latest_semver }}"
          CURRENT="${{ steps.version.outputs.current }}"
          BRANCH="auto-update/cliamp-${LATEST_SEMVER}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add data/cliamp.json flake.lock README.md
          git commit -m "chore: add cliamp ${LATEST}"
          git push --force origin "$BRANCH"

          PR_URL=$(gh pr create \
            --title "chore: add cliamp ${LATEST}" \
            --body "Automated addition of cliamp \`${LATEST}\` (previous newest: \`v${CURRENT}\`).

          Changes:
          - \`data/cliamp.json\`: new entry with \`srcHash\` and \`vendorHash\`
          - \`README.md\`: current-version link updated to \`${LATEST}\`

          Installable as \`#cliamp_$(printf '%s' "$LATEST_SEMVER" | tr '.' '_')\`; \`cliamp\`/\`default\` now track it.

          Build and smoke-test (\`cliamp -v\`) verified." \
            --base main \
            --head "$BRANCH")

          gh pr merge --squash "$PR_URL"

          # Keep pushing git tags so legacy ?ref=vX.Y.Z pins remain available.
          git checkout main
          git pull
          git tag "$LATEST"
          git push origin "$LATEST"
